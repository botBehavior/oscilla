[package]
name = "oscilla-verify"
version = "0.1.0"
edition = "2021"

[dependencies]
oscilla-synth = { path = "../synth" }
glam = { version = ">=0.30.8, <0.31" }
wgpu = { version = "29", features = ["spirv"] }
pollster = "0.4"
bytemuck = "1"
